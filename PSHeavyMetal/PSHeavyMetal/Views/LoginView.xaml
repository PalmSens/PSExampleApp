<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:PSHeavyMetal.Forms.ViewModels" x:DataType="viewmodels:LoginViewModel"
             x:Class="PSHeavyMetal.Forms.Views.LoginView"
             Visual="Material"
             BackgroundImageSource="background.jpg"
             Title="PS Heavy Metal v0.1">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="WELCOME"
                   HorizontalOptions="CenterAndExpand"
                   VerticalOptions="Start"
                   Margin="0,100,0,0"
                   FontSize="Large" />
            <Label Text="Please turn on the reader to continue"
                   WidthRequest="200"
                   HorizontalOptions="CenterAndExpand"
                   VerticalOptions="Start"
                   Margin="10,10,0,20"
                   FontSize="Medium" />
            <StackLayout Orientation="Horizontal"
                             HorizontalOptions="CenterAndExpand"
                             VerticalOptions="CenterAndExpand">
                <Picker Grid.Column="1"
                        WidthRequest="200"
                        ItemsSource="{Binding Users}"
                        ItemDisplayBinding="{Binding Name}" />
                <ImageButton Grid.Column="2"
                    WidthRequest="30"
                    HeightRequest="30"
                    BackgroundColor="Transparent"
                    Aspect="Fill"
                    Source="plusicon.png"
                    Command="{Binding OpenAddUserViewCommand}"
                    VerticalOptions="Center"
                    HorizontalOptions="Center" />
            </StackLayout>
            <Button Text="Continue" Command="{Binding LoginCommand}"
                    Margin="100"
                    HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>