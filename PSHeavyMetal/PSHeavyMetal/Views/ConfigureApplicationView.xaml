<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PSHeavyMetal.Forms.Views.ConfigureApplicationView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:PSHeavyMetal.Forms.ViewModels"
    xmlns:views="clr-namespace:PSHeavyMetal.Forms.Views"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    x:DataType="viewmodels:ConfigureApplicationViewModel"
    Style="{StaticResource CustomPage}">
    <ContentPage.Content>
        <Grid>
            <Label
                Grid.Row="0"
                Style="{StaticResource LargeTextStyle}"
                Text="{xct:Translate AdminOptionsHeader}"
                VerticalOptions="CenterAndExpand"
                WidthRequest="250" />
            <StackLayout
                Grid.Row="1"
                HorizontalOptions="CenterAndExpand"
                Spacing="25"
                VerticalOptions="CenterAndExpand"
                WidthRequest="225">
                <Button Command="{Binding ConfigureAnalyteCommand}" Text="{xct:Translate ConfigureAnalyte}" />
                <Button Command="{Binding ConfigureMethodCommand}" Text="{xct:Translate ConfigureMethod}" />
            </StackLayout>
            <ContentView Grid.Row="2" VerticalOptions="End">
                <views:StatusBarView />
            </ContentView>
        </Grid>
    </ContentPage.Content>
</ContentPage>