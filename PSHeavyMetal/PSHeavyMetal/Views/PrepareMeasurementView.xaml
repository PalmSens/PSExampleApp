<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PSHeavyMetal.Forms.Views.PrepareMeasurementView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:resources="clr-namespace:PSHeavyMetal.Forms.Resx"
    xmlns:viewmodels="clr-namespace:PSHeavyMetal.Forms.ViewModels"
    xmlns:views="clr-namespace:PSHeavyMetal.Forms.Views"
    x:DataType="viewmodels:PrepareMeasurementViewModel"
    Style="{StaticResource CustomPage}">

    <Grid>
        <StackLayout
            HorizontalOptions="CenterAndExpand"
            Orientation="Vertical"
            VerticalOptions="CenterAndExpand">
            <Entry
                Margin="0,0,0,20"
                Placeholder="{x:Static resources:AppResources.SampleName}"
                Text="{Binding SampleName}" />
            <Editor
                AutoSize="Disabled"
                HeightRequest="150"
                MaxLength="300"
                MinimumHeightRequest="150"
                Placeholder="{x:Static resources:AppResources.Notes}"
                Text="{Binding SampleNotes}" />
            <Button
                Margin="0,20,0,0"
                Command="{Binding ContinueCommand}"
                Text="{x:Static resources:AppResources.Continue}" />
        </StackLayout>

        <ContentView VerticalOptions="End">
            <views:StatusBarView />
        </ContentView>
    </Grid>
</ContentPage>