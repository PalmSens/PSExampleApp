<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:PSHeavyMetal.Forms.ViewModels"
             x:DataType="viewmodels:AddUserViewModel"
             x:Class="PSHeavyMetal.Forms.Views.AddUserView"
             Visual="Material">
    <ContentPage.Content>
        <Grid>
            <StackLayout VerticalOptions="CenterAndExpand"
                     HorizontalOptions="CenterAndExpand"
                     Orientation="Vertical">                
                <Entry Text="{Binding UserName}" Placeholder="Enter a user name"/>
            </StackLayout>
            <StackLayout VerticalOptions="EndAndExpand" 
                         HorizontalOptions="CenterAndExpand" 
                         Margin="0,0,0,75" 
                         Spacing="20" >
                <Button Text="Add user" Command="{Binding AddUserCommand}"/>
                <Button Text="Cancel" Command="{Binding CancelCommand}" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>